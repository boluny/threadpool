# Following are for build inside the source tree
# CMake generated files
*.cmake
CMakeCache.txt
CMakeFiles/
# We use CMake for this repo so no makefile needed
Makefile

# Following for my personal build path
build/

